# Legacy Pages Folder

This top-level `pages/` directory is leftover residue from the older Streamlit-based version of LogSentinel. It is not part of the active monorepo runtime.

## Current Source Of Truth

- The active browser UI now lives in `frontend/src/pages/`.
- The active orchestration layer now lives in `mlcore/engine/`.
- The active API entrypoints now live in `backend/api/`.

This folder remains in the checkout as historical residue only. Treat it as archival, not operational.