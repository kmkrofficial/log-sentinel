# Changelog

All notable repository-level documentation changes are recorded here.

## 2026-07-09

### Added

- Added folder-level README files for `engine/`, `pages/`, `prepareData/`, and `utils/`.
- Added architecture, runtime-flow, and design-rationale documentation for the main repository structure.

### Changed

- Rewrote the root README to document the current execution model, generated runtime directories, data contract, and top-level scripts.
- Corrected the high-level project structure to reflect `executions/` and `logsentinel_data/` instead of the older `reports/` wording.